class Screen_Position:
    def __init__(self, x, y):
        # the position of the screen
        self.position = (x, y)

    def get_pos(self):
        # current position in memory
        return self.position
    
    def update_pos(self,x ,y)
        # updates the position
        self.position = (x, y)